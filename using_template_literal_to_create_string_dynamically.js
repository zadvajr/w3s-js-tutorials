//using template literal to dynamically create strings

const product = "Laptop";

const price = 1200;

const purchaseDetails = `You have purchased ${product} for the price of ${price} dollars.`;
console.log(purchaseDetails);